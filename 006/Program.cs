double Sin(double x) //метод
{
    return Math.Sin(x);
}
double Sum(double a, double b ) //метод
{
    return Math.Sin(x);
}

void Pause() //метод
{
    System.Console.WriteLine("press any key");
}

double y=Sin(3.14) //метод
System.Console.WriteLine(y);
Pause();